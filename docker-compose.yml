services:
  cardinal:
    build:
      context: ./cardinal
    ports:
      - "8081:80"
    volumes:
      - ./cardinal:/code
      - ./logs:/code/logs
    env_file:
      - ./cardinal/.env
    networks:
      - mynetwork
    user: root

networks:
  mynetwork:
    driver: bridge